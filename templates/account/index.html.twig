{% extends 'base.html.twig' %}

{% block title %}Mon compte{% endblock %}

{% block body %}
<div class="container mt-4">
    <div class="row">
        <div class="col-md-8 mx-auto">
            <div class="d-flex justify-content-between align-items-center mb-4">
                <h2 class="mb-0">👤 Mon compte</h2>
                <div>
                    <a href="{{ path('app_account_edit') }}" class="btn btn-outline-primary btn-sm me-2" title="Modifier mes informations">
                        <i class="fas fa-edit"></i> Modifier le profil
                    </a>
                    <a href="{{ path('app_account_password') }}" class="btn btn-outline-warning btn-sm" title="Changer le mot de passe">
                        <i class="fas fa-key"></i> Changer le mot de passe
                    </a>
                </div>
            </div>

            <!-- Informations personnelles -->
            <div class="card mb-4 shadow-sm">
                <div class="card-header d-flex justify-content-between align-items-center">
                    <h5 class="mb-0"><i class="fas fa-user"></i> Informations personnelles</h5>
                    <a href="{{ path('app_account_edit') }}" class="btn btn-link p-0" title="Modifier">
                        <i class="fas fa-pencil-alt text-primary"></i>
                    </a>
                </div>
                <div class="card-body">
                    <div class="row">
                        <div class="col-sm-4">
                            <strong>Email :</strong>
                        </div>
                        <div class="col-sm-8">
                            {{ user.email }}
                        </div>
                    </div>
                    <hr>
                    <div class="row">
                        <div class="col-sm-4">
                            <strong>Nom :</strong>
                        </div>
                        <div class="col-sm-8">
                            {{ user.nom ?? 'Non défini' }}
                        </div>
                    </div>
                    <hr>
                    <div class="row">
                        <div class="col-sm-4">
                            <strong>Prénom :</strong>
                        </div>
                        <div class="col-sm-8">
                            {{ user.prenom ?? 'Non défini' }}
                        </div>
                    </div>
                    <hr>
                    <div class="row">
                        <div class="col-sm-4">
                            <strong>Rôle :</strong>
                        </div>
                        <div class="col-sm-8">
                            <span class="badge bg-primary">{{ user.roles[0] ?? 'ROLE_USER' }}</span>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Sécurité -->
            <div class="card mb-4 shadow-sm">
                <div class="card-header d-flex justify-content-between align-items-center">
                    <h5 class="mb-0"><i class="fas fa-shield-alt"></i> Sécurité</h5>
                    <a href="{{ path('app_account_password') }}" class="btn btn-link p-0" title="Changer le mot de passe">
                        <i class="fas fa-pencil-alt text-warning"></i>
                    </a>
                </div>
                <div class="card-body">
                    <div class="row">
                        <div class="col-sm-4">
                            <strong>Mot de passe :</strong>
                        </div>
                        <div class="col-sm-8">
                            <span class="text-muted">••••••••</span>
                            <small class="text-muted d-block">
                                Dernière modification : 
                                {% if user.passwordUpdatedAt %}
                                    {{ user.passwordUpdatedAt|date('d/m/Y à H:i') }}
                                {% else %}
                                    <span class="text-warning">Jamais modifié</span>
                                {% endif %}
                            </small>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}